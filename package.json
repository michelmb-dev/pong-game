{
	"name": "pong-game",
	"description": "Pong Game JS",
	"author": "mmichelmb-dev",
	"license": "ISC",
	"version": "1.0.0",
	"scripts": {
		"start": "rm -rf dist && rm -rf .parcel-cache && parcel src/index.html --open",
		"build": "rm -rf dist && rm -rf .parcel-cache && parcel build src/index.html --no-source-maps --no-cache",
		"clear": "rm -rf build && rm -rf .parcel-cache && rm -rf dist"
	},
	"devDependencies": {
		"@parcel/core": "^2.0.1",
		"@parcel/transformer-sass": "^2.0.1",
		"autoprefixer": "^10.4.1",
		"parcel": "^2.0.1",
		"postcss": "^8.4.5"
	}
}
